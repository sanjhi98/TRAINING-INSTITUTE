CREATE TABLE TRAINER(TID INT NOT NULL AUTO_INCREMENT,
     UID VARCHAR(12) NOT NULL,
     AGE INT,
     SPECIALIZATION VARCHAR(255) NOT NULL,
     USERNAME VARCHAR(20) NOT NULL,
     PASSWORD VARCHAR(255) NOT NULL,
	NAME VARCHAR(20) NOT NULL,
     CONSTRAINT tid_PK PRIMARY KEY(TID));

 ALTER TABLE TRAINER AUTO_INCREMENT=200;

CREATE TABLE ENROLLMENT(ENROLLID INT NOT NULL AUTO_INCREMENT,
     BATCH_ID INT NOT NULL,
     STUDENT_ID INT NOT NULL,
     PRIMARY KEY(ENROLLID),
     FOREIGN KEY(BATCH_ID) REFERENCES BATCH(BID),
     FOREIGN KEY(STUDENT_ID) REFERENCES STUDENT(SID));

 ALTER TABLE ENROLLMENT AUTO_INCREMENT=500;

CREATE TABLE STUDENT(SID INT NOT NULL AUTO_INCREMENT,
     NAME VARCHAR(50) NOT NULL,
     FATHER_NAME VARCHAR(50),
     UID VARCHAR(12) NOT NULL,
     AGE INT,USERNAME VARCHAR(20) NOT NULL,
     PASSWORD VARCHAR(255) NOT NULL,
     CONSTRAINT SID_PK PRIMARY KEY(SID));

 ALTER TABLE STUDENT AUTO_INCREMENT=100;

CREATE TABLE COURSE(CID INT NOT NULL AUTO_INCREMENT,
     COURSE_NAME VARCHAR(50) NOT NULL,
     PRIMARY KEY(CID));

 ALTER TABLE COURSE AUTO_INCREMENT=300;
CREATE TABLE BATCH(BID INT NOT NULL AUTO_INCREMENT,
     COURSE_ID INT NOT NULL,
     START_DATE DATE NOT NULL,
     END_DATE DATE NOT NULL,
     COURSE_FEE FLOAT(7,2) NOT NULL,
	TID INT NOT NULL,
     PRIMARY KEY(BID),
       FOREIGN KEY(TID) REFERENCES TRAINER(TID));

ALTER TABLE BATCH AUTO_INCREMENT=400;
CREATE TABLE PAYMENT(SID INT NOT NULL,
	STATUS VARCHAR(20) NOT NULL,
	FOREIGN KEY(SID) REFERENCES STUDENT(SID));


     FOREIGN KEY(COURSE_ID) REFERENCES COURSE(CID));